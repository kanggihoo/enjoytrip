# EnjoyTrip 유스케이스 다이어그램


---

## 그림 1 · 관광지 조회 Usecase (F101~103)

```mermaid
graph LR
    사용자(["👤 사용자"])

    subgraph SYS ["관광지 조회 시스템"]
        direction TB
        UC1(["관광지 검색"])
        UC2(["관광지 상세 조회"])
        UC3(["지도 마커 확인"])
        UC4(["시/도 · 구군 선택"])
    end

    사용자 --> UC1
    사용자 --> UC2
    사용자 --> UC3
    UC1 -. "<<include>>" .-> UC4
```

---

## 그림 2 · 여행계획 관리 Usecase (F104)

```mermaid
graph LR
    사용자(["👤 사용자"])

    subgraph SYS ["여행계획 관리 시스템"]
        direction TB
        UC1(["여행 계획 등록"])
        UC2(["여행 계획 수정"])
        UC3(["여행 계획 삭제"])
        UC4(["여행 계획 목록"])
        UC5(["여행 목적지 관광지 검색"])
    end

    관리자(["👤 관리자"])

    사용자 --> UC1
    사용자 --> UC2
    사용자 --> UC3
    사용자 --> UC4
    UC1 -. "<<include>>" .-> UC5
    UC5 --- 관리자
```

---

## 그림 3 · 핫플레이스 Usecase (F105)

```mermaid
graph LR
    사용자(["👤 사용자"])
    비회원(["👤 비회원"])

    subgraph SYS ["핫플레이스 시스템"]
        direction TB
        UC1(["핫플레이스 목록 조회"])
        UC2(["핫플레이스 상세 조회"])
        UC3(["핫플레이스 등록"])
        UC4(["핫플레이스 수정"])
        UC5(["핫플레이스 삭제"])
    end

    비회원 --> UC1
    비회원 --> UC2
    사용자 --> UC1
    사용자 --> UC2
    사용자 --> UC3
    사용자 --> UC4
    사용자 --> UC5
```

---

## 그림 4 · 게시판 Usecase (F106)

```mermaid
graph LR
    사용자(["👤 사용자"])
    비회원(["👤 비회원"])

    subgraph SYS ["게시판 시스템"]
        direction TB
        UC1(["공지사항 조회"])
        UC2(["자유게시판 조회"])
        UC3(["게시글 작성"])
        UC4(["게시글 수정"])
        UC5(["게시글 삭제"])
    end

    관리자(["👤 관리자"])

    비회원 --> UC1
    비회원 --> UC2
    사용자 --> UC1
    사용자 --> UC2
    사용자 --> UC3
    사용자 --> UC4
    사용자 --> UC5
    UC1 --- 관리자
    UC3 --- 관리자
    UC4 --- 관리자
    UC5 --- 관리자
```

---

## 그림 5 · 기본관리 Usecase (F107~108)

```mermaid
graph LR
    사용자(["👤 사용자"])

    subgraph SYS ["기본관리 시스템"]
        direction TB
        UC1(["회원 가입"])
        UC2(["회원 정보 조회"])
        UC3(["회원 정보 수정"])
        UC4(["로그인 / 로그아웃"])
        UC5(["비밀번호 변경"])
        UC6(["공지사항 검색"])
        UC7(["공지사항 등록"])
        UC8(["공지사항 수정"])
        UC9(["공지사항 삭제"])
    end

    회원탈퇴(["회원 탈퇴"])
    관리자(["👤 관리자"])

    사용자 --> UC1
    사용자 --> UC2
    사용자 --> UC3
    사용자 --> UC4
    사용자 --> UC6
    사용자 --> 회원탈퇴
    UC4 -. "<<include>>" .-> UC5
    관리자 --> UC7
    관리자 --> UC8
    관리자 --> UC9
    관리자 --> UC6
```
